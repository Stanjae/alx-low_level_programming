#include "main.h"
/**
 *_abs - print the absolute value of an integer
 * @n: the parameter of the function
 * Return: return abs of the int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
