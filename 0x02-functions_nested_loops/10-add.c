#include "main.h"
/**
 * add - adds two digit and return the result
 * @a: first parameter
 * @b: second parameter
 * Return: returns the sum
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
