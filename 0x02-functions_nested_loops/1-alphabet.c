#include "main.h"
/**
 * print_alphabet - prints all the alphabet in lowercase
 */
void print_alphabet(void)
{
	char soak;

	for (soak = 'a'; soak <= 'z'; soak++)
	{
		_putchar(soak);
	}
	_putchar('\n');
}
